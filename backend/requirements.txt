fastapi==0.115.*
uvicorn[standard]==0.30.*
sqlalchemy==2.0.*
pydantic==2.*
PyJWT==2.9.*
anthropic>=0.40.0
python-multipart
python-dotenv
httpx>=0.27

# Optional enterprise engines (heavy: torch + model weights, several GB).
# The platform runs fully without them via the native rails/optimizer runtime.
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install llmlingua nemoguardrails
# NeMo's LLM self-check rail (Gemini via langchain):
#   pip install langchain langchain-community langchain-google-genai
# Presidio PII masking (NER-based, spaCy):
#   pip install presidio-analyzer presidio-anonymizer
#   python -m spacy download en_core_web_sm
